# User-agent: * means this applies to all search engine bots (Google, Bing, etc.)
User-agent: *

# Leaving Disallow blank means absolutely nothing is restricted!
Disallow:

# Explicitly tell bots they are allowed to crawl and index your room paths
Allow: /
Allow: /room/
Allow: /room/*